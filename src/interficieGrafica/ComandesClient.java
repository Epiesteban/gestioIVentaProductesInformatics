package interficieGrafica;

import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

public class ComandesClient implements ActionListener {
		private ProgramaPrincipal finestra;

		/**
		 * Constructor de l'acció.
		 * @param finestra referencia a la finestra principal
		 */
		public ComandesClient (ProgramaPrincipal finestra) {
			this.finestra = finestra;
		}

		/**
		 * Acció que s'executa al prèmer el botó de mostrar les comandes de l'usuari.
		 */
		public void actionPerformed(ActionEvent e) {
			///finestra.;
		}
	}
